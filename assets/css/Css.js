import { StyleSheet } from "react-native";

const css = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      justifyContent: 'center',
    },

    map: {
        height: '70%'
    },

    search: {
        height: 50,
    },

    distancia: {
      marginTop: -80
    }
  });

  export {css};